@model IEnumerable<BEFOYS.DataLayer.Model.TblProductFeatureGroup>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PfgId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PfgName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PfgGuid)
            </th>
            <th></th>
        </tr>
    </thead>

    @foreach (var item in Model)
    {

        <tr>
            <td>@item.PfgName</td>
        </tr>
        <tbody>
            @foreach (var item2 in item.TblProductFeatures)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item2.PfId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item2.PfName)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td>
                </tr>
            }

        </tbody>
    }

</table>
